<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.ImageEditor;assembly=Syncfusion.Maui.ImageEditor"
             x:Class="OcrMaui.MainPage"
             BackgroundColor="Black">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Text="OCR MEDIDORES"
                TextColor="White"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            <Grid>
                <syncfusion:SfImageEditor x:Name="imageEditor" IsVisible="False"/> 
                <Button
                x:Name="CounterBtn"
                Text="Pick Image" 
                SemanticProperties.Hint="Picks an image for OCR"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="PictureBtn"
                Text="Take Picture" 
                SemanticProperties.Hint="Takes a picture for OCR"
                Clicked="PictureBtn_Clicked"
                HorizontalOptions="Fill" />

            </Grid>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
